var isContain = function(array, year, month, day) {
  //数组里去找这个id，找得到返回true,找不到返回false
  for(var i = 0; i < array.length; i++){
    var ymd = array[i].split("-");
    //if()
  }
  
  return list.indexOf(item) == -1 ? false : true
  }
  module.exports.isContain = isContain
